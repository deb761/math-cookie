@model IEnumerable<Solstice.Models.AddSubProblem>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Operator1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Operator2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProblemType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Result)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Level)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Operator1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Operator2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProblemType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Result)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.AddSubProblemID">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.AddSubProblemID">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.AddSubProblemID">Delete</a>
        </td>
    </tr>
}
</table>
